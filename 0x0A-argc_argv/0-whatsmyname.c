#include <stdio.h>
/**
 * main- program entry point
 * @argc: main parameter known as argument count;
 * @argv: also a parameter known as argument vector;
 * Return: (0) at success;
 */

int main(int argc, char *argv[])
{
	if (argc == 1)
	{
	printf("%s\n", argv[0]);
	}
return (0);
}
